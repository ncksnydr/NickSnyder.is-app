/**
 *  Stylelint → Rules → Avoid errors → Overrides
 *  @see https://stylelint.io/user-guide/rules/#overrides
 */

module.exports = {
	rules: {
		'declaration-block-no-shorthand-property-overrides': true
	}
};
