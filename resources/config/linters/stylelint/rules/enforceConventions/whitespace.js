/**
 *  Stylelint → Rules → Enforce conventions → Whitespace
 *  @see https://stylelint.io/user-guide/rules#whitespace-inside
 */

module.exports = {
	rules: {
		'comment-whitespace-inside': 'always'
	}
};
